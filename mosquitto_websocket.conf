# Minimal Mosquitto config with WebSocket support
per_listener_settings true

# TCP listener (port 1883)
listener 1883
protocol mqtt
allow_anonymous true

# WebSocket listener (port 9001) - Required for browser/Tauri apps
listener 9001
protocol websockets
allow_anonymous true
